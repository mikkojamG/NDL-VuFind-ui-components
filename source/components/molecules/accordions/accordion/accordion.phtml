<?php
$iconClass = isset($icon) ? $icon :  'fa fa-handle-open';
$open = isset($initallyOpen) && $initallyOpen;
?>

<div class="collapse-accordion<?= (isset($darkTheme) && $darkTheme) ? ' collapse-accordion-dark' : ''; ?><?= (isset($outline) && $outline) ? ' collapse-accordion-outline' : ''; ?><?= $wrapperClass ?? ''; ?>">
  <button type="button" id="<?= $triggerId; ?>" class="collapse-accordion-trigger<?= $open ? '' : ' collapsed'; ?>" data-toggle="collapse" data-target="#<?= $contentId; ?>" aria-expanded="<?= $open ? 'true' : 'false'; ?>" aria-controls="<?= $contentId; ?>">
    <?php if (!isset($iconAlign)) : ?>
      <i class="<?= $iconClass; ?>" aria-hidden="true"></i>
    <?php endif; ?>
    <?= $triggerText; ?>
    <?php if (isset($iconAlign) && $iconAlign == 'right') : ?>
      <i class="<?= $iconClass; ?> pull-right" aria-hidden="true"></i>
    <?php endif; ?>
  </button>
  <section id="<?= $contentId; ?>" aria-expanded="<?= $open ? 'true' : 'false'; ?>" class="collapse-accordion-panel collapse<?= $open ? ' in' : '';?>" aria-labelledby="<?= $triggerId; ?>">
    <?= $content; ?>
  </section>
</div>
