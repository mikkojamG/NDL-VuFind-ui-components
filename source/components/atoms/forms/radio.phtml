<div class="c-input-container">
  <fieldset class="form-group">
    <legend class="hidden"><?= $inputRadioGroupLabel; ?></legend>
    <ul>
      <?php foreach ($inputRadioGroup as $input) : ?>
        <li class="radio">
          <?= $this->render('components/atoms/forms/input/_radio.phtml', ['inputName' => $input['key'], 'groupName' => $inputRadioGroupName, 'value' => $input['value'], 'checked' => isset($input['checked']) ? $input['checked'] : false]) ?>
        </li>
      <?php endforeach; ?>
    </ul>
  </fieldset>
</div>
